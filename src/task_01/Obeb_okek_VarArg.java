/*package task_01;

import java.util.Scanner;

public class Obeb_okek_VarArg {

	/*
	 * Function to return gcd of a and b
	 
	static int gcd(int a, int b) {
		if (a == 0)
			return b;
		return gcd(b % a, a);
	}

	// Function to find gcd of array of
	// numbers
	static int findGCD(int arr[], int n) {
		int result = arr[0];
		for (int i = 1; i < n; i++)
			result = gcd(arr[i], result);

		return result;
	}

	public static void main(String[] args) 
        { 
        	System.out.println("sayý gir");
        	
        	for(int i = 0;i < 50; i++) {
			Scanner input = new Scanner(System.in);
			int c = input.nextInt();
			
			
			
			
			 * 
			 * int arr[] = new int[c];
			 
        	
            //int arr[] = { 2, 4, 6, 20, 16 }; 
            int n = e.length(); 
            System.out.println(findGCD(arr, n)); 
        } }
}
      

 * 
 * int a, b, i, max, min, Ebob = 1, Ekok = 1; Scanner Klavye = new
 * Scanner(System.in);// Kalvyeden deðer alan java nesnesi
 * System.out.println("EBOB ve EKOK hesabý için iki deðer giriniz.");
 * System.out.print("Ýlk deðeri giriniz = "); a = Klavye.nextInt();
 * System.out.print("Ýkinci deðeri giriniz = "); b = Klavye.nextInt();
 * 
 * max = (a > b) ? a : b; min = (a > b) ? b : a;
 * 
 * for (i = max; i >= min; i--) { if ((min % i == 0) && (max % i == 0)) { Ebob =
 * Ebob * i; } Ekok = (a * b) / Ebob; } System.out.println("EBOB = " + Ebob +
 * " EKOK = " + Ekok );
 */
